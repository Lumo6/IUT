n = input('Entrez la valeur de n : ');
X = grand(1, n, 'nor', 5, 1);

classes = linspace(0, 10, 21);

histplot(c,X,normalization=%f, style=5)

x = linspace(0, 10, 100);
y = 1 / (sqrt(2 * %pi)) * exp(-0.5 * (((x - 5) / 1).^2));

plot(x, y, color='red');

